{
  "name": "tools",
  "version": "1.0.0",
  "scripts": {
    "tslint": "tslint --project tsconfig.json",
    "clean": "rm -rf build",
    "clean:all": "npm run clean; rm -rf node_modules;",
    "html2jsonml": "ts-node ./src/html2jsonml-cli.ts"
  },
  "dependencies": {
    "sax": "^1.2.4",
    "ts-node": "^3.3.0"
  },
  "devDependencies": {
    "@types/sax": "^1.0.0",
    "tslint": "^5.8.0"
  }
}
